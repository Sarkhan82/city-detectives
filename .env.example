# City Detectives – variables d'environnement (Story 1.2)
# Copier en .env et adapter. Ne pas committer .env.

# Flutter / frontend (pas de chargement .env natif : utiliser --dart-define au build)
# Ex. : flutter run --dart-define=API_BASE_URL=https://api.example.com
API_BASE_URL=http://localhost:8080

# Backend (city-detectives-api)
# JWT_SECRET=change-me-in-production
# DATABASE_URL=postgres://user:pass@localhost:5432/city_detectives
